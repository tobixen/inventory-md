We have documentation multiple places now:

* `~/tingbok`
* `~/inventory-md` (including `~/inventory-md/claude-skills/`)
* `~/.claude/projects/-home-tobias-*inventory*/memory/`

There is a lot of duplicated information.  Please look through all of it (including TODO-files) and consider ways to consolidate it.

The "claude skills" directory under inventory-md should only contain generic things applying to any user of the inventory-md system.  All references to files under /home/tobias and personal things like "update the diary with expenses" should be moved (back) into ~/.claude/skills - and then my personal skill should rather have things in it like "continue following the guide under ~/inventory-md/claude-skills/add-items.md`".

Some things I often need help for is:

 * Post-shopping: read a receipt, either from a json file or from a photo file, add expenses to a diary and items into the inventory.
 * Process photos of a container or a storage.
 * A combination of those (when I've done shopping and I've photographed the new items in the new location)
 * General maintenance of the inventory files

Probably the guides on categories, quantities etc should be yanked from the maintenance guide and put into a separate how-to-document on how to add new items - and then the maintenance guide should refer to that document.  The skill files may also refer to that document.
